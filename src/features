import { createSlice } from '@reduxjs/toolkit'

const initialState = {
    loading: true,
    countries: [],
    error:'',
}

export const  countriesSlice = createSlice({
    name: 'countries',
    initialState,
    reducers:{

    }

})